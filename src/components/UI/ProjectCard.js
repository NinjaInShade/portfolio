import React from "react";
import "./ProjectCard.css";

export default function ProjectCard({ img, title, description, live_URL, github_URL, wave1, wave2, reversed, white }) {
  return (
    <div className="project-card">
      <div className="project-card-bg"></div>
    </div>
  );
}
